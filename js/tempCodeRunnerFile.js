// 1

let ageTable = document.getElementById('age-table');

let labelElem = document.querySelectorAll('label');

let firstTd = document.querySelector('td');

let requiredForm = document.getElementsByName('search');

let firstInput = document.label.firstElementChild;

let lastInput = document.label.lastElementChild;

console.log(ageTable, labelElem, firstTd, requiredForm, firstInput, lastInput);